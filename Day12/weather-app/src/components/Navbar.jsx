function Navbar(){
    return (
        <nav className="navbar">
            <h2>Weather App</h2>
            <p>Search for weather using City names</p>
        </nav>
    );
}

export default Navbar;
